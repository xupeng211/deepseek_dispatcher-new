# Dockerfile-web

# 使用官方 Python 基础镜像
FROM python:3.10-slim-buster

# 设置工作目录
WORKDIR /app

# 复制 requirements.txt 到容器中，并安装依赖
# 使用 --no-cache-dir 避免在镜像中保留 pip 缓存，减小镜像大小
# 使用 --upgrade pip 确保 pip 是最新版本
COPY requirements.txt .
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir -r requirements.txt

# 复制整个应用程序代码到容器中
COPY . .

# 暴露 Flask 应用的端口
EXPOSE 8000

# 定义容器启动时运行的命令
# 使用 waitress-serve 来运行 Flask 应用
# --call 参数告诉 waitress 调用 create_app() 函数来获取 Flask 应用实例
# --host 和 --port 从环境变量获取，确保与 docker-compose.yml 中的映射一致
CMD ["waitress-serve", "--host", "0.0.0.0", "--port", "8000", "--call", "web.app:create_app"]
