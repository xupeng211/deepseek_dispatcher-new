# Dockerfile-worker
# Worker 也可以使用相同的 Python 基础镜像
FROM python:3.10-slim-buster

# 设置工作目录
WORKDIR /app

# 复制 requirements.txt
COPY requirements.txt .

# 安装 Python 依赖
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir -r requirements.txt

# 复制整个项目代码
COPY . .

# 定义容器启动时执行的命令
# 这里直接运行 worker/worker.py 脚本，它会启动 RQ Worker
# 注意：--with-scheduler 参数在 worker/worker.py 中已经通过 worker.work(with_scheduler=True) 启用
# 你也可以直接在这里使用 `rq worker` 命令，但运行自定义脚本更灵活
CMD ["python", "worker/worker.py"]

# 如果你更倾向于直接使用 rq 命令，可以这样写：
# CMD ["rq", "worker", "--url", "redis://redis:6379/0", "deepseek_tasks"]
# 但我们已经在 worker/worker.py 中处理了这些配置，所以推荐使用上面的 python 命令