# 核心依赖
pydantic-settings
fastapi==0.110.1
uvicorn[standard]==0.29.0 # 建议固定版本
redis==5.0.3
rq==1.16.1
rq-scheduler==0.13.1

# LLM 相关
openai==1.35.1 # 如果用到 OpenAI API，固定版本
requests==2.32.3 # 如果代码中直接使用 requests，固定版本
httpx==0.27.0 # 如果用到 httpx，固定版本
tiktoken==0.6.0 # 如果用到 token 计数，固定版本

# 日志记录
loguru==0.7.2

# 实用工具
click==8.1.7
python-dotenv==1.0.1