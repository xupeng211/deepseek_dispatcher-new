# requirements.txt
# Flask==3.1.1 # 迁移到 FastAPI 后，可以考虑移除 Flask
# gunicorn==22.0.0 # 迁移到 FastAPI 后，可以考虑移除 gunicorn
# waitress==3.0.0 # 迁移到 FastAPI 后，可以考虑移除 waitress
redis==5.0.1
rq==2.3.3
python-dotenv==1.0.1
structlog==24.1.0
requests==2.31.0
dashscope==1.14.0 # DeepSeek API 客户端

# 新增：OpenAI 库
openai==1.30.0 # 推荐使用最新版本，这里提供一个示例版本

# FastAPI 和 Uvicorn
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1 # 用于更高级的设置管理，可选

# 关键改动：确保 httpx 版本支持 'proxies' 参数
# openai 1.x.x 版本依赖 httpx，并需要其支持 proxies 参数
# httpx 0.20.0 版本引入了 Client.proxies 属性
# 为了避免 AttributeError: 'Client' object has no attribute 'proxies'，我们确保其版本足够新
# **重要：由于当前环境中 httpx>=0.30.0 无法找到，我们将其版本锁定为当前可用的最新版本 0.28.1**
httpx==0.27.2