# requirements.txt
# Python Web 框架
Flask>=3.1.0

# HTTP 请求库 (OpenAI SDK 内部可能使用，显式列出以防万一)
requests>=2.32.3

# 环境变量加载
python-dotenv>=1.1.0

# OpenAI 兼容 API 客户端 (用于 DashScope)
openai>=1.0.0

# Redis 队列系统
rq>=1.0.0
rq-scheduler>=0.11.0 # 用于 RQ 的定时任务调度器

# Redis 客户端 (推荐安装 hiredis 以提高性能)
redis[hiredis]>=4.0.0

# 数据验证和序列化
pydantic>=2.0.0

# Flask API 限流 (包含 Redis 存储后端)
Flask-Limiter[redis]

# 进程管理工具 (主要用于 Docker 容器内管理 Worker 进程)
supervisor

# WSGI 服务器 (用于生产环境运行 Flask 应用)
waitress